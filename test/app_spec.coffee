
_      = require 'underscore'
chai   = require 'chai'

expect = chai.expect


describe 'Easel Backbone View', ->

   it 'Should accept a c.Stage reference on instantiation', ->

   it 'Should create a base c.Container instance', ->

   it 'Should add the container to the main stage on render', ->

   it 'Should remove the container from the main stage on remove', ->

   it 'Should add all passed in children to the container', ->

   it 'Should bind all custom DisplayObject events', ->

   it 'Should unbind all DisplayObject events on remove', ->


